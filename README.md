# papermind
PaperMind · 全场景在线考试平台，覆盖基础教育、高等院校、职业资格认证与企业知识考核。

## 本地开发命令

项目根目录提供 `Makefile` 作为常用命令入口：

```bash
make frontend-build  # 打包前端
make frontend-dev    # 启动前端开发服务
make backend-build   # 编译后端到 server/bin/papermind.exe
make backend-dev     # 启动后端服务
```
